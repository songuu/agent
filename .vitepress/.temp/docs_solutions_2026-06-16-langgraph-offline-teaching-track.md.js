import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"离线确定性 LangGraph 教学轨道（六件套原子 + 旋钮无关 invariant + 复核揪教学谬误）","description":"","frontmatter":{"title":"离线确定性 LangGraph 教学轨道（六件套原子 + 旋钮无关 invariant + 复核揪教学谬误）","date":"2026-06-16T00:00:00.000Z","tags":["solution","langgraph","curriculum","offline-deterministic","knowledge-graph","teaching"],"related_instincts":["teaching-demo-deterministic-payoff","concept-visual-kind-hardcoded-captions","kg-data-driven-doc-generation"],"aliases":["langgraph 课程怎么离线讲","纯函数节点教 StateGraph","并行产出顺序不保证 措辞","多 agent 离线教学"]},"headers":[],"relativePath":"docs/solutions/2026-06-16-langgraph-offline-teaching-track.md","filePath":"docs/solutions/2026-06-16-langgraph-offline-teaching-track.md","lastUpdated":1781570464000}');
const _sfc_main = { name: "docs/solutions/2026-06-16-langgraph-offline-teaching-track.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="离线确定性-langgraph-教学轨道" tabindex="-1">离线确定性 LangGraph 教学轨道 <a class="header-anchor" href="#离线确定性-langgraph-教学轨道" aria-label="Permalink to &quot;离线确定性 LangGraph 教学轨道&quot;">​</a></h1><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>要给初学者仓库新增「进阶 LangGraph 专题」5 章（StateGraph / 条件边 / checkpointer / human-in-the-loop / 多 agent），但 LangGraph 教程默认都要接 LLM（需 key、结果不确定、不可回归），与本仓库「离线确定教学」伦理冲突。同时新章必须满足知识图谱「六件套原子契约」（<code>CONCEPT_VISUALS.length===CHAPTERS.length</code> 等测试守门）。</p><h2 id="root-cause" tabindex="-1">Root Cause <a class="header-anchor" href="#root-cause" aria-label="Permalink to &quot;Root Cause&quot;">​</a></h2><p>LangGraph 的<strong>核心价值根本不在 LLM</strong>：StateGraph、channels+reducers、条件边/循环、Send 扇出、checkpointer 持久化、interrupt 恢复、多 agent 拓扑——<strong>节点就是普通函数</strong>，用纯函数节点（确定规则、不调模型）即可把这些机制全讲透，<code>needsKey:&quot;none&quot;</code>、无 key 也能跑、结果确定可回归。把「人」「agent」抽象成确定值（resume 值 / 纯函数 worker），审批两路径、多 agent 两拓扑全部离线确定。</p><h2 id="solution" tabindex="-1">Solution <a class="header-anchor" href="#solution" aria-label="Permalink to &quot;Solution&quot;">​</a></h2><p><strong>离线建轨范式（每章六件套原子完成）</strong>：</p><ol><li><code>src/shared/langgraph/*.ts</code> 纯函数节点图 + barrel 导出；worker 与 demo 期望<strong>共用同一纯函数核</strong>（如 <code>computeTaskResult</code>）→ 零漂移。</li><li>demo <code>index.ts</code>：每条结论用 <code>invariant()</code> 运行时硬核对，且<strong>全部旋钮无关</strong>——断言<strong>构造性质</strong>（<code>2×STEP×INVOKES</code>、单调递减、线程隔离、顺序保持、集合相等、重放复现、<code>supervise===tasks+1</code>），期望由纯函数核现场重算，<strong>不写死具体数</strong>。改练习常量不得让 demo 误报崩。</li><li>README：手写 mermaid + <code>## 五、小结与延伸</code> 锚点（<code>npm run kg</code> 在该锚点前注入 KG 段）。</li><li>smoke.ts 累加断言。</li><li>数据源 <code>graph.ts</code>（CHAPTERS+CONCEPTS+RELATIONS+ARTICLES）+ <code>visuals.ts</code>（CONCEPT_VISUALS+HIGHLIGHTS）。</li><li>CONCEPT_VISUALS kind 每章<strong>选不同且 steps 驱动</strong>避主题串味：pipeline/loop/stream/shield/fusion（<strong>避开 space=ch08、compare=ch10 硬编码文案画布</strong>）。</li></ol><p><strong>0.2.74 API 离线坐实（勿照搬记忆，每章先 spike 实跑）</strong>：</p><ul><li>checkpointer：<code>compile({checkpointer:new MemorySaver()})</code> + <code>invoke(input,{configurable:{thread_id}})</code> 跨 invoke 持久化；<code>invoke({})</code> 续跑合法；累积靠 <strong>reducer（sum/append）非 checkpointer 本身</strong>；<code>getStateHistory</code> 倒序；time-travel=<code>invoke(null, 历史entry.config)</code>。</li><li>interrupt：<code>result.__interrupt__</code>=undefined（顶层不暴露）；<strong>payload 读法=<code>getState(cfg).tasks[].interrupts[].value</code></strong>；resume=<code>invoke(new Command({resume}),cfg)</code>；<strong>坑</strong>=暂停时普通 <code>invoke(input)</code> 不 resume，会带新输入从头重跑。</li></ul><p><strong>Phase 4 对抗复核揪出的「教学谬误」类缺陷（高复用警示）</strong>：</p><ul><li><strong>顺序无关的归因</strong>：<code>append reducer</code> <strong>保留到达顺序、不消除顺序</strong>；map-reduce 汇总「顺序无关」真正来自<strong>聚合运算可交换</strong>（求和 a+b===b+a）。顺序敏感运算（拼接）必须先排序。别把顺序无关归到 append。</li><li><strong>「不保证」是契约级 ≠ 运行时每次乱</strong>：纯同步纯函数 demo 的并行收集顺序其实<strong>每次确定</strong>。所以「去掉 sort 多跑几次观察不稳定」的练习<strong>不会复现</strong>（违反「练习现象须可见」不变量）。正确表述：契约上不承诺=边书写顺序、不可依赖；要复现需改 <code>async</code>+不同延时。</li><li><strong>过度概括</strong>：「多个节点往 replace channel 写就互相覆盖」只对<strong>先后串行写</strong>成立；<strong>同一步并行</strong>写同一 replace channel 实际抛 <code>InvalidUpdateError</code>，非静默覆盖。</li></ul><h2 id="prevention" tabindex="-1">Prevention <a class="header-anchor" href="#prevention" aria-label="Permalink to &quot;Prevention&quot;">​</a></h2><ul><li>新建任何「机制教学」轨道，先问「这机制的本质需要外部依赖吗」——LangGraph/RAG 大量机制纯逻辑即可演示，离线优先。</li><li>教学 demo 的每条「现象」都要能被构造保证或现场计算坐实；练习改的常量必须让现象<strong>真实可见</strong>（写完实跑核对，别假设）。</li><li>涉及「顺序/并行/聚合」的措辞，分清三件事：<strong>收集顺序</strong>（reducer 行为）、<strong>聚合是否可交换</strong>（运算性质）、<strong>契约保证 vs 本地观测</strong>（不可依赖本地确定性）。三者常被混为一谈，是教学谬误高发区。</li><li>提交前对抗式复核（skeptic 默认 refuted）专治这类「单测/build 全绿但教学讲错」——纯函数测不到措辞正确性。</li><li>共享生成器（generate.ts）的改动若影响并行轨道，<strong>推迟 + flag</strong>，不擅动。</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li>[[teaching-demo-deterministic-payoff]] — 教学 demo 结论须确定性，本轨道 invariant 旋钮无关的延续</li><li>[[concept-visual-kind-hardcoded-captions]] — kind 选择避 space/compare 硬编码文案</li><li>[[kg-data-driven-doc-generation]] — 单一数据源 + 幂等注入派生 KG</li><li>[[precommit-adversarial-review-catches-dom-races]] — 提交前对抗复核揪全绿漏网缺陷（本轮揪教学谬误同理）</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("docs/solutions/2026-06-16-langgraph-offline-teaching-track.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260616LanggraphOfflineTeachingTrack = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260616LanggraphOfflineTeachingTrack as default
};
