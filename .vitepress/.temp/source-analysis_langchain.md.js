import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"LangChain 源码解析","description":"","frontmatter":{},"headers":[],"relativePath":"source-analysis/langchain.md","filePath":"source-analysis/langchain.md","lastUpdated":1782716344000}');
const _sfc_main = { name: "source-analysis/langchain.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="langchain-源码解析" tabindex="-1">LangChain 源码解析 <a class="header-anchor" href="#langchain-源码解析" aria-label="Permalink to &quot;LangChain 源码解析&quot;">​</a></h1><blockquote><p>一句话：LangChain 的主线不是“一个 agent 类”，而是 <code>Runnable</code> 组合能力、agent factory、middleware、structured output、LangGraph runtime 这几层叠出来的开发体验。</p></blockquote><p>源码入口按 2026-06-29 官方仓库主干核对：</p><table tabindex="0"><thead><tr><th>入口</th><th>读什么</th></tr></thead><tbody><tr><td><a href="https://github.com/langchain-ai/langchain/blob/master/libs/langchain_v1/langchain/agents/factory.py" target="_blank" rel="noreferrer"><code>libs/langchain_v1/langchain/agents/factory.py</code></a></td><td><code>create_agent</code> 如何组装模型、工具、middleware、response format 和图 runtime</td></tr><tr><td><a href="https://github.com/langchain-ai/langchain/blob/master/libs/langchain_v1/langchain/agents/middleware/types.py" target="_blank" rel="noreferrer"><code>libs/langchain_v1/langchain/agents/middleware/types.py</code></a></td><td>middleware 在模型调用、工具调用、状态更新前后插入扩展点</td></tr><tr><td><a href="https://github.com/langchain-ai/langchain/blob/master/libs/langchain_v1/langchain/agents/structured_output.py" target="_blank" rel="noreferrer"><code>libs/langchain_v1/langchain/agents/structured_output.py</code></a></td><td>provider-native structured output 与 tool-call structured output 的策略分流</td></tr><tr><td><a href="https://github.com/langchain-ai/langchain/blob/master/libs/core/langchain_core/runnables/base.py" target="_blank" rel="noreferrer"><code>libs/core/langchain_core/runnables/base.py</code></a></td><td><code>Runnable</code> 的 <code>invoke</code> / <code>batch</code> / <code>stream</code> / composition 基础协议</td></tr></tbody></table><h2 id="先拆仓库层次" tabindex="-1">先拆仓库层次 <a class="header-anchor" href="#先拆仓库层次" aria-label="Permalink to &quot;先拆仓库层次&quot;">​</a></h2><p>LangChain 现在要分清三层：</p><table tabindex="0"><thead><tr><th>层</th><th>作用</th><th>读源码时的判断</th></tr></thead><tbody><tr><td><code>langchain_core</code></td><td>最底层协议，放 messages、tools、runnables、callbacks 等共用抽象</td><td>这是稳定心智模型，优先读。</td></tr><tr><td><code>langchain_v1</code></td><td>新 agent 入口与更现代的 agent runtime 包装</td><td>读 <code>create_agent</code> 主线。</td></tr><tr><td><code>langchain_classic</code> / 旧链路</td><td>兼容旧 chains、classic agents、legacy retrievers</td><td>只在维护老项目时回头读。</td></tr></tbody></table><p>这意味着读 LangChain 不要从“某个 Chain 类”开始，而要从 <code>Runnable</code> 和 <code>create_agent</code> 开始。</p><h2 id="主链路-create-agent-做了什么" tabindex="-1">主链路：<code>create_agent</code> 做了什么 <a class="header-anchor" href="#主链路-create-agent-做了什么" aria-label="Permalink to &quot;主链路：\`create_agent\` 做了什么&quot;">​</a></h2><p>可以把 <code>create_agent</code> 理解成一个装配函数：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>用户参数</span></span>
<span class="line"><span>  -&gt; 规范化 model / tools / middleware / response_format</span></span>
<span class="line"><span>  -&gt; 把模型调用包成 agent node</span></span>
<span class="line"><span>  -&gt; 把工具执行包成 tool node</span></span>
<span class="line"><span>  -&gt; 把结构化输出策略接入模型或工具调用</span></span>
<span class="line"><span>  -&gt; 生成可循环的 graph runtime</span></span>
<span class="line"><span>  -&gt; 返回一个可 invoke / stream 的 agent</span></span></code></pre></div><p>关键判断：LangChain agent 的“循环”已经不再只是一个手写 <code>while</code>，而是把循环下沉到图 runtime。公开 API 看起来还是 <code>agent.invoke(...)</code>，内部要读成“编译好的图在执行”。</p><h2 id="runnable-是底层通用接口" tabindex="-1"><code>Runnable</code> 是底层通用接口 <a class="header-anchor" href="#runnable-是底层通用接口" aria-label="Permalink to &quot;\`Runnable\` 是底层通用接口&quot;">​</a></h2><p>如果你只读 agent 层，会漏掉 LangChain 的真正统一口径：<code>Runnable</code>。</p><table tabindex="0"><thead><tr><th>能力</th><th>为什么重要</th></tr></thead><tbody><tr><td><code>invoke</code></td><td>单输入单输出，是所有 runnable 的最低执行面。</td></tr><tr><td><code>batch</code></td><td>同一 runnable 批量执行，框架可以统一调度并发。</td></tr><tr><td><code>stream</code></td><td>流式输出不是 agent 特例，而是 runnable 协议的一部分。</td></tr><tr><td>composition</td><td>prompt、model、parser、retriever 可以组合成链，减少胶水代码。</td></tr></tbody></table><p>对照本课程：第 02 章的 <code>LLMClient.chat/stream</code> 是最小接口；LangChain 把这个接口扩大成所有组件的统一协议。</p><h2 id="middleware-是生产扩展点" tabindex="-1">middleware 是生产扩展点 <a class="header-anchor" href="#middleware-是生产扩展点" aria-label="Permalink to &quot;middleware 是生产扩展点&quot;">​</a></h2><p>middleware 的价值不是“多一个 hook”，而是把生产横切逻辑从业务 prompt 里拿出来：</p><table tabindex="0"><thead><tr><th>hook 类型</th><th>常见用途</th></tr></thead><tbody><tr><td>model 前后</td><td>动态 system prompt、模型选择、token 预算、trace metadata。</td></tr><tr><td>tool 前后</td><td>权限检查、参数改写、错误标准化、敏感工具审批。</td></tr><tr><td>state 前后</td><td>压缩消息、注入上下文、清理中间状态。</td></tr></tbody></table><p>对照第 06 章：我们手写 <code>ToolRegistry.run()</code> 时把校验和错误回传放在本地边界。LangChain middleware 把这些边界做成可组合插件。</p><h2 id="structured-output-是两条路" tabindex="-1">structured output 是两条路 <a class="header-anchor" href="#structured-output-是两条路" aria-label="Permalink to &quot;structured output 是两条路&quot;">​</a></h2><p>读 <code>structured_output.py</code> 时重点看策略分流，而不是背类名：</p><table tabindex="0"><thead><tr><th>策略</th><th>适合场景</th><th>风险</th></tr></thead><tbody><tr><td>Provider strategy</td><td>模型/厂商原生支持结构化输出</td><td>依赖 provider 能力，迁移时要重新验证。</td></tr><tr><td>Tool strategy</td><td>把 schema 伪装成 tool call</td><td>通用性强，但会占用 tool-call 路径，错误修复要更明确。</td></tr></tbody></table><p>对照第 13 章：课程里用 zod + retry-repair 把 JSON 变成可测试契约；LangChain 则把“怎么让模型吐结构化对象”抽成策略。</p><h2 id="读源码的顺序" tabindex="-1">读源码的顺序 <a class="header-anchor" href="#读源码的顺序" aria-label="Permalink to &quot;读源码的顺序&quot;">​</a></h2><ol><li>打开 <code>factory.py</code>，先找 <code>create_agent</code> 的参数如何被规范化。</li><li>找 model node 和 tool node 的创建点，确认工具调用回路在哪里闭合。</li><li>跳到 <code>structured_output.py</code>，看 response format 如何进入模型调用或工具调用。</li><li>跳到 middleware types，看 hook 能插入哪些生命周期位置。</li><li>最后读 <code>Runnable</code>，把 agent、chain、retriever、parser 都放回统一调用协议。</li></ol><h2 id="和手写版的对照" tabindex="-1">和手写版的对照 <a class="header-anchor" href="#和手写版的对照" aria-label="Permalink to &quot;和手写版的对照&quot;">​</a></h2><table tabindex="0"><thead><tr><th>本课程手写层</th><th>LangChain 对应层</th></tr></thead><tbody><tr><td><code>runAgent</code> 的循环</td><td>agent graph runtime</td></tr><tr><td><code>ToolRegistry</code></td><td>tools + tool node + middleware</td></tr><tr><td><code>LLMClient.chat/stream</code></td><td><code>Runnable.invoke/stream</code> + chat model</td></tr><tr><td>JSON schema + retry</td><td>structured output strategy</td></tr><tr><td><code>onStep</code> / console trace</td><td>callback / tracing / middleware</td></tr></tbody></table><h2 id="自检问题" tabindex="-1">自检问题 <a class="header-anchor" href="#自检问题" aria-label="Permalink to &quot;自检问题&quot;">​</a></h2><ul><li><code>create_agent</code> 返回的对象为什么能 <code>invoke</code>，它背后接的是哪类 runtime？</li><li>工具执行失败时，错误是在工具层、middleware 层还是模型层被处理？</li><li>structured output 走 provider-native 和 tool-call 两条路时，哪条更容易迁移？</li><li>如果要加“高风险工具必须审批”，应该改 prompt、改 tool，还是加 middleware？</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("source-analysis/langchain.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const langchain = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  langchain as default
};
