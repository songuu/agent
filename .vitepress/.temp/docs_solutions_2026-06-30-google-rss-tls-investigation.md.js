import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Google AI / DeepMind RSS TLS 排查与解决方案","description":"","frontmatter":{},"headers":[],"relativePath":"docs/solutions/2026-06-30-google-rss-tls-investigation.md","filePath":"docs/solutions/2026-06-30-google-rss-tls-investigation.md","lastUpdated":1782809182000}');
const _sfc_main = { name: "docs/solutions/2026-06-30-google-rss-tls-investigation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="google-ai-deepmind-rss-tls-排查与解决方案" tabindex="-1">Google AI / DeepMind RSS TLS 排查与解决方案 <a class="header-anchor" href="#google-ai-deepmind-rss-tls-排查与解决方案" aria-label="Permalink to &quot;Google AI / DeepMind RSS TLS 排查与解决方案&quot;">​</a></h1><p>日期：2026-06-30</p><h2 id="结论" tabindex="-1">结论 <a class="header-anchor" href="#结论" aria-label="Permalink to &quot;结论&quot;">​</a></h2><p><code>google-ai</code> 与 <code>deepmind</code> 的反复失败不是 RSS 内容格式问题，也不是 Supabase 写入问题。当前机器对 Google 域名的 DNS 被改写到 <code>198.18.0.0/15</code> fake-IP，并且 <code>HTTP_PROXY</code> / <code>HTTPS_PROXY</code> / <code>ALL_PROXY</code> 指向 <code>http://127.0.0.1:9</code> 这个无效端口。失败发生在本机网络出口 / 代理 / TUN 层，表现为 TLS handshake/reset：</p><ul><li><code>Client network socket disconnected before secure TLS connection was established</code></li><li><code>curl: schannel: failed to receive handshake, SSL/TLS connection failed</code></li><li><code>curl: Send failure: Connection was aborted</code></li></ul><p>因此，完整解决要分两层：</p><ul><li>仓库代码层：增强 collector 对 <code>rss-parser.parseURL()</code> TLS 失败的恢复能力，避免单个请求路径脆弱导致直接丢源。</li><li>运行环境层：修正 Google 域名的代理/DNS 出口；否则所有 <code>*.google</code> / <code>*.googleblog.com</code> 官方 feed 都可能继续握手失败。</li></ul><h2 id="已验证证据" tabindex="-1">已验证证据 <a class="header-anchor" href="#已验证证据" aria-label="Permalink to &quot;已验证证据&quot;">​</a></h2><h3 id="_1-dns-被-fake-ip-tun-接管" tabindex="-1">1. DNS 被 fake-IP/TUN 接管 <a class="header-anchor" href="#_1-dns-被-fake-ip-tun-接管" aria-label="Permalink to &quot;1. DNS 被 fake-IP/TUN 接管&quot;">​</a></h3><p>命令：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Resolve-DnsName</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> blog.google</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Resolve-DnsName</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> deepmind.google</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Resolve-DnsName</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> blog.google </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Server </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">8.8</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">8.8</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Resolve-DnsName</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> deepmind.google </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Server </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">8.8</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">8.8</span></span></code></pre></div><p>结果都落到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>blog.google      A 198.18.0.140</span></span>
<span class="line"><span>deepmind.google  A 198.18.0.141</span></span></code></pre></div><p><code>198.18.0.0/15</code> 是保留测试网段，通常来自本地代理/TUN fake-IP，不是 Google 真实公网解析。</p><h3 id="_2-代理环境变量无效" tabindex="-1">2. 代理环境变量无效 <a class="header-anchor" href="#_2-代理环境变量无效" aria-label="Permalink to &quot;2. 代理环境变量无效&quot;">​</a></h3><p>命令：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Get-ChildItem</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Env:</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">proxy</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span></span></code></pre></div><p>结果：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HTTP_PROXY=http://127.0.0.1:9</span></span>
<span class="line"><span>HTTPS_PROXY=http://127.0.0.1:9</span></span>
<span class="line"><span>ALL_PROXY=http://127.0.0.1:9</span></span>
<span class="line"><span>GIT_HTTP_PROXY=http://127.0.0.1:9</span></span>
<span class="line"><span>GIT_HTTPS_PROXY=http://127.0.0.1:9</span></span></code></pre></div><p><code>127.0.0.1:9</code> 不是可用代理端口。Node 内置 <code>fetch</code> 默认不自动使用这些代理变量，但这个配置仍说明当前 shell 网络环境不是正常直连态。</p><h3 id="_3-rss-地址状态" tabindex="-1">3. RSS 地址状态 <a class="header-anchor" href="#_3-rss-地址状态" aria-label="Permalink to &quot;3. RSS 地址状态&quot;">​</a></h3><ul><li><code>https://deepmind.google/blog/rss.xml</code>：间歇成功。修复后真实 <code>fetchFeed()</code> 曾在第 3 次尝试成功，返回 <code>100</code> 条，首条为 <code>Introducing computer use in Gemini 3.5 Flash</code>。</li><li><code>https://blog.google/technology/ai/rss/</code>：旧地址会 <code>301</code> 到 <code>https://blog.google/innovation-and-ai/technology/ai/rss/</code>。</li><li><code>https://blog.google/innovation-and-ai/technology/ai/rss/</code>：当前环境仍会 TLS reset。</li><li><code>https://blog.google/rss/</code>、<code>https://research.google/blog/rss/</code>、<code>https://ai.googleblog.com/feeds/posts/default</code>、<code>https://developers.googleblog.com/feeds/posts/default/-/AI</code>：同样走 Google 域，当前环境下也会 TLS reset。</li></ul><h2 id="已落地的代码修复" tabindex="-1">已落地的代码修复 <a class="header-anchor" href="#已落地的代码修复" aria-label="Permalink to &quot;已落地的代码修复&quot;">​</a></h2><h3 id="_1-google-ai-blog-改成-canonical-rss" tabindex="-1">1. Google AI Blog 改成 canonical RSS <a class="header-anchor" href="#_1-google-ai-blog-改成-canonical-rss" aria-label="Permalink to &quot;1. Google AI Blog 改成 canonical RSS&quot;">​</a></h3><p>文件：<code>news-collector/src/sources.ts</code></p><ul><li>主 URL：<code>https://blog.google/innovation-and-ai/technology/ai/rss/</code></li><li>fallback URL：<code>https://blog.google/technology/ai/rss/</code></li></ul><p>原因：旧 URL 已经 301，主路径改成 canonical 可减少一次重定向和一个失败点。</p><h3 id="_2-rss-parser-parseurl-失败后增加-fetch-parsestring-fallback" tabindex="-1">2. <code>rss-parser.parseURL()</code> 失败后增加 <code>fetch + parseString</code> fallback <a class="header-anchor" href="#_2-rss-parser-parseurl-失败后增加-fetch-parsestring-fallback" aria-label="Permalink to &quot;2. \`rss-parser.parseURL()\` 失败后增加 \`fetch + parseString\` fallback&quot;">​</a></h3><p>文件：<code>news-collector/src/rss.ts</code></p><p>逻辑：</p><ol><li>对 source 主 URL 先走 <code>rss-parser.parseURL()</code>。</li><li>如果遇到 TLS/socket/网络错误，再用 Node <code>fetch()</code> 拉 XML。</li><li>用现有 <code>parseFeedString()</code> 解析 XML。</li><li>如果主 URL 失败，再尝试 <code>fallbackUrls</code>。</li><li>所有失败详情进入 <code>error</code> / <code>diagnostics</code>，包含 URL、抓取方法和最后错误。</li></ol><p>这能覆盖 <code>rss-parser</code> HTTP 栈偶发失败但 Node fetch 能成功的场景。它不能绕过整个 Google 域都被本机代理层阻断的场景。</p><h3 id="_3-单测覆盖" tabindex="-1">3. 单测覆盖 <a class="header-anchor" href="#_3-单测覆盖" aria-label="Permalink to &quot;3. 单测覆盖&quot;">​</a></h3><p>文件：<code>news-collector/__tests__/rss.test.mts</code></p><p>新增用例：</p><ul><li>模拟 <code>parseURL</code> 抛 <code>Client network socket disconnected before secure TLS connection was established</code>。</li><li>模拟 <code>globalThis.fetch</code> 返回 RSS XML。</li><li>验证 <code>fetchFeed()</code> 第 1 次尝试恢复成功。</li></ul><p>旧重试用例也已更新：<code>parseURL</code> 与 fallback <code>fetch</code> 同时失败时，仍按 source retry policy 重试 5 次并返回 <code>ok:false</code>。</p><h2 id="验证结果" tabindex="-1">验证结果 <a class="header-anchor" href="#验证结果" aria-label="Permalink to &quot;验证结果&quot;">​</a></h2><p>相关测试通过：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">node node_modules\\tsx\\dist\\cli.mjs </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">test news</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">collector\\__tests__\\rss.test.mts news</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">collector\\__tests__\\sources.test.mts news</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">collector\\__tests__\\collect.test.mts</span></span></code></pre></div><p>结果：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tests 14</span></span>
<span class="line"><span>pass 14</span></span>
<span class="line"><span>fail 0</span></span></code></pre></div><p>真实探针结果：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deepmind ok=true attempts=3 count=100</span></span>
<span class="line"><span>google-ai ok=false attempts=5</span></span></code></pre></div><p><code>deepmind</code> 已受益于重试 + fallback；<code>google-ai</code> 当前仍受本机 Google 域 TLS 出口故障影响。</p><h2 id="环境侧最小修复动作" tabindex="-1">环境侧最小修复动作 <a class="header-anchor" href="#环境侧最小修复动作" aria-label="Permalink to &quot;环境侧最小修复动作&quot;">​</a></h2><h3 id="方案-a-修正本机代理端口" tabindex="-1">方案 A：修正本机代理端口 <a class="header-anchor" href="#方案-a-修正本机代理端口" aria-label="Permalink to &quot;方案 A：修正本机代理端口&quot;">​</a></h3><p>如果本机有 Clash / Mihomo / sing-box / v2rayN 等代理，先确认真实 HTTP 代理端口，常见端口为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>127.0.0.1:7890</span></span>
<span class="line"><span>127.0.0.1:7897</span></span>
<span class="line"><span>127.0.0.1:10809</span></span></code></pre></div><p>然后在运行 collector 的 shell 中设置为真实端口，例如：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">env:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">HTTP_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;http://127.0.0.1:7890&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">env:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">HTTPS_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;http://127.0.0.1:7890&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">env:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ALL_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;http://127.0.0.1:7890&quot;</span></span></code></pre></div><p>如果没有可用本地代理，不要保留 <code>127.0.0.1:9</code> 这类假代理值：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Remove-Item</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Env:HTTP_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ErrorAction SilentlyContinue</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Remove-Item</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Env:HTTPS_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ErrorAction SilentlyContinue</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">Remove-Item</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Env:ALL_PROXY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ErrorAction SilentlyContinue</span></span></code></pre></div><h3 id="方案-b-修正-tun-dns-规则" tabindex="-1">方案 B：修正 TUN / DNS 规则 <a class="header-anchor" href="#方案-b-修正-tun-dns-规则" aria-label="Permalink to &quot;方案 B：修正 TUN / DNS 规则&quot;">​</a></h3><p>当前 <code>blog.google</code> 和 <code>deepmind.google</code> 都解析到 fake-IP。需要在代理客户端里确认：</p><ul><li>TUN 模式正在运行。</li><li>fake-IP 映射能被代理进程正确接管。</li><li><code>.google</code>、<code>.googleblog.com</code>、<code>blog.google</code>、<code>deepmind.google</code> 走代理规则，而不是被错误直连或丢弃。</li><li>如果使用 fake-IP，确保 collector 进程与代理 TUN 在同一网络命名空间和同一 Windows 用户会话下运行。</li></ul><h3 id="方案-c-生产服务器单独修复" tabindex="-1">方案 C：生产服务器单独修复 <a class="header-anchor" href="#方案-c-生产服务器单独修复" aria-label="Permalink to &quot;方案 C：生产服务器单独修复&quot;">​</a></h3><p>如果生产机也失败，不要只在本地改。到生产机跑同样探针：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -e</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;for (const u of [&#39;https://blog.google/innovation-and-ai/technology/ai/rss/&#39;,&#39;https://deepmind.google/blog/rss.xml&#39;]) { try { const r=await fetch(u,{signal:AbortSignal.timeout(12000)}); console.log(u,r.status,r.headers.get(&#39;content-type&#39;)); } catch(e) { console.log(u,e.message,e.cause?.code,e.cause?.message); } }&quot;</span></span></code></pre></div><p>生产 PM2 / systemd 环境需要显式注入代理变量或修 DNS/TUN；不要依赖交互式 shell 的代理设置。</p><h2 id="后续监控建议" tabindex="-1">后续监控建议 <a class="header-anchor" href="#后续监控建议" aria-label="Permalink to &quot;后续监控建议&quot;">​</a></h2><ol><li>保留 <code>google-ai</code> / <code>deepmind</code> 为 enabled，因为两个源不是 feed 失效，而是当前出口不稳定。</li><li>每次日报里继续记录 <code>diagnostics</code>，现在会包含主 URL、fallback URL、<code>parseURL</code> 与 <code>fetch+parseString</code> 的具体失败路径。</li><li>如果连续 7 天 <code>google-ai</code> 仍失败，增加“Google AI Blog 官方内容替代源”审查任务，但不要直接用 <code>blog.google/rss/</code> 全站 feed 替代 AI 分类 feed，避免非 AI 内容污染。</li><li>生产侧优先修代理/DNS；代码 fallback 只能处理单请求栈失败，不能绕过整个 Google 域出口不可达。</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("docs/solutions/2026-06-30-google-rss-tls-investigation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260630GoogleRssTlsInvestigation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260630GoogleRssTlsInvestigation as default
};
