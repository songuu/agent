import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"图表缩放：定位漂移修复 + contain 首屏 + 全屏弹层","description":"","frontmatter":{"title":"图表缩放：定位漂移修复 + contain 首屏 + 全屏弹层","date":"2026-06-11T00:00:00.000Z","tags":["solution","diagram-zoom","vitepress","dom","no-vue"],"related_instincts":["dom-transform-pollutes-measurement"],"aliases":["mermaid 图被裁","图表位置不对","diagram 全屏查看"]},"headers":[],"relativePath":"docs/solutions/2026-06-11-diagram-zoom-contain-fullscreen.md","filePath":"docs/solutions/2026-06-11-diagram-zoom-contain-fullscreen.md","lastUpdated":1781169085000}');
const _sfc_main = { name: "docs/solutions/2026-06-11-diagram-zoom-contain-fullscreen.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="图表缩放-定位漂移修复-contain-首屏-全屏弹层" tabindex="-1">图表缩放：定位漂移修复 + contain 首屏 + 全屏弹层 <a class="header-anchor" href="#图表缩放-定位漂移修复-contain-首屏-全屏弹层" aria-label="Permalink to &quot;图表缩放：定位漂移修复 + contain 首屏 + 全屏弹层&quot;">​</a></h1><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>课程页可缩放 Mermaid 图表（<code>.vitepress/theme/diagram-zoom.ts</code>）用户反馈两点：①「位置不对」——开屏左上一大片空白、内容挤到右下被裁；②「需要尽可能多的显示内容」——宽图开屏不全。硬约束：vanilla TS + DOM，<strong>禁止 Vue</strong>。</p><h2 id="root-cause" tabindex="-1">Root Cause <a class="header-anchor" href="#root-cause" aria-label="Permalink to &quot;Root Cause&quot;">​</a></h2><ol><li><strong>定位漂移</strong>：<code>getDiagramBounds</code> 经 <code>svg.getBoundingClientRect()</code> 推导像素尺度，而该方法<strong>把元素自身 CSS transform 算进去</strong>（<code>getBBox()</code> 不会）。fit 一旦应用过 <code>scale(S)</code>，下一次测量被放大 S 倍；又因 fit 会写 <code>surface.style.height</code> → 触发 <code>ResizeObserver</code> 重新进入 fit，逐轮复合放大、定位漂移。非幂等循环。</li><li><strong>显示不全</strong>：旧策略 <code>READABLE_INITIAL_SCALE</code>=0.9→1.0「宽图固定 100% 开屏、横向溢出靠拖」，与「尽可能多显示」直接冲突——宽图 100% 必然裁右侧。这条旧规则（见 debugging-gotchas 2026-06-10「readable initial scale should win」）本轮被<strong>显式反转</strong>为 contain。</li></ol><h2 id="solution" tabindex="-1">Solution <a class="header-anchor" href="#solution" aria-label="Permalink to &quot;Solution&quot;">​</a></h2><p><code>.vitepress/theme/diagram-zoom.ts</code> + <code>custom.css</code> + <code>diagram-zoom.test.mts</code>：</p><ul><li><strong>测前归零 transform</strong>：<code>const prev = el.style.transform; el.style.transform=&quot;none&quot;; const b = getDiagramBounds(el); el.style.transform=prev;</code> 消污染。</li><li><strong>幂等写回守门</strong>：<code>if (surface.style.height !== nextHeight) surface.style.height = nextHeight;</code> 相同输入→相同输出，RO 第二次拿到同值不再 fire，收敛。</li><li><strong>contain 首屏</strong>：<code>scale = clamp(min(widthScale, heightScale), MIN_CONTAIN_SCALE=0.5, INITIAL_MAX_SCALE=1.8)</code>，整图可见、居中；面板高度自适应并按视口收窄（<code>viewportSurfaceHeightCap</code>，≤640px 压到 320 对齐移动端 @media）。</li><li><strong>方向性溢出渐隐提示</strong>（<code>has-overflow-x/y</code>）：按当前平移量判定「前方还有内容」，拖到尽头自动关、不谎报；pointer-events 透明不挡拖动。</li><li><strong>全屏弹层</strong>（替代不可行的 breakout）：工具栏「⤢ 全屏查看」→ 克隆 SVG 放进覆盖全视口的遮罩大面板，<code>calculateContainedFit</code> 在定宽高盒里 contain 到最大（下限 0.45、上限 2.5），独立 zoom/pan，Esc/遮罩/✕ 关闭，焦点陷阱 + 滚动锁 + 监听清理 + close 幂等守门。完全隔离于内联缩放，不竞态。</li><li><strong>加固</strong>：单纯点击不再永久锁 <code>hasUserTransform</code>（仅真正位移后锁，否则 resize 不再自动 fit）；NaN 用否定式比较 <code>!(w&gt;0)</code> 走兜底（避免 <code>scale(NaN)</code>）；<code>document.fonts.ready</code> 后补一次 fit；新增 <code>transition</code> 镜像进 <code>prefers-reduced-motion</code>。</li></ul><h2 id="prevention" tabindex="-1">Prevention <a class="header-anchor" href="#prevention" aria-label="Permalink to &quot;Prevention&quot;">​</a></h2><ul><li>任何「测 DOM 几何来布局」的代码：先确认 <code>getBoundingClientRect</code> 是否被自身/祖先 transform 污染；要测「未变换尺寸」就先归零 transform 再测。fit 改尺寸又被 RO 回调重入时，务必让 fit 幂等。</li><li>视觉策略二选一（完整 vs 易读）由<strong>用户当轮指令</strong>定，别自己反复横跳；冲突时给「contain 看全 + zoom/全屏看细节」的组合，而不是单点取舍。</li><li>横向 breakout 在「正文列左贴侧栏、右贴固定 TOC」的 VitePress 布局里不可行（撞侧栏/TOC）；要「更大」用全屏弹层（仅动图表、零布局冲突），不要硬改列宽。</li><li>新增动画/<code>transition</code> 即使逃过 CSS-diff 守门也要镜像进 <code>prefers-reduced-motion</code>（见 [[reduced-motion-mirror-guard]]）。</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li><code>node node_modules\\tsx\\dist\\cli.mjs .vitepress\\theme\\diagram-zoom.test.mts</code>（含 contain/纵向溢出/纵向居中/<code>calculateContainedFit</code> 数值断言）</li><li><code>node node_modules\\tsx\\dist\\cli.mjs .vitepress\\theme\\reduced-motion.test.mts</code></li><li><code>pnpm typecheck</code> + 生产 <code>vitepress build</code>（base <code>/agent-build/</code>）</li><li>对抗式复核：contain workflow（3 视角逐条验真）+ UX 回归 + 弹层复核，confirmed findings 全部已修或显式记录原因</li><li>两次部署上线 <a href="https://songuu.top/agent-build%EF%BC%88HTTPS" target="_blank" rel="noreferrer">https://songuu.top/agent-build（HTTPS</a> 全 200、loop_scene=1；回滚备份 dist.bak.20260611162429 / 20260611164550）</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li>[[dom-transform-pollutes-measurement]] — 本能：测量被 transform 污染 + RO 重入复合的根因与三件套修法</li><li>[[precommit-adversarial-review-catches-dom-races]] — DOM 单例/测量/竞态类提交前固定对抗式复核</li><li>[[2026-06-10-course-visual-polish]] — 上一轮图表视觉打磨（被本轮 contain 策略反转）</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("docs/solutions/2026-06-11-diagram-zoom-contain-fullscreen.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260611DiagramZoomContainFullscreen = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260611DiagramZoomContainFullscreen as default
};
