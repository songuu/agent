import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"LlamaIndex 源码解析","description":"","frontmatter":{},"headers":[],"relativePath":"source-analysis/llamaindex.md","filePath":"source-analysis/llamaindex.md","lastUpdated":1782716344000}');
const _sfc_main = { name: "source-analysis/llamaindex.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="llamaindex-源码解析" tabindex="-1">LlamaIndex 源码解析 <a class="header-anchor" href="#llamaindex-源码解析" aria-label="Permalink to &quot;LlamaIndex 源码解析&quot;">​</a></h1><blockquote><p>一句话：LlamaIndex 是 data-first 的 agent/RAG 框架。它的主线不是先问“agent 怎么循环”，而是先问“数据如何入索引、如何检索、如何合成答案，再如何接进 workflow/agent”。</p></blockquote><p>用户输入里的 <code>lamdaIndex</code> 这里按业界常用项目名 <strong>LlamaIndex</strong> 处理。</p><p>源码入口按 2026-06-29 官方仓库主干核对：</p><table tabindex="0"><thead><tr><th>入口</th><th>读什么</th></tr></thead><tbody><tr><td><a href="https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/query_engine/retriever_query_engine.py" target="_blank" rel="noreferrer"><code>llama-index-core/llama_index/core/query_engine/retriever_query_engine.py</code></a></td><td>query engine 如何把 retriever、node postprocessor、response synthesizer 串起来</td></tr><tr><td><a href="https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/agent/workflow/base_agent.py" target="_blank" rel="noreferrer"><code>llama-index-core/llama_index/core/agent/workflow/base_agent.py</code></a></td><td>agent workflow 的 base agent、tool、handoff、event 基础</td></tr><tr><td><a href="https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/agent/workflow/multi_agent_workflow.py" target="_blank" rel="noreferrer"><code>llama-index-core/llama_index/core/agent/workflow/multi_agent_workflow.py</code></a></td><td>多 agent workflow 如何围绕 events、handoff 和 current agent 状态推进</td></tr><tr><td><a href="https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/workflow/workflow.py" target="_blank" rel="noreferrer"><code>llama-index-core/llama_index/core/workflow/workflow.py</code></a></td><td>通用 workflow runtime：step、event、context、run</td></tr></tbody></table><h2 id="先用-data-first-心智读它" tabindex="-1">先用 data-first 心智读它 <a class="header-anchor" href="#先用-data-first-心智读它" aria-label="Permalink to &quot;先用 data-first 心智读它&quot;">​</a></h2><p>LangChain 更像“组件组合框架”，LangGraph 更像“状态机 runtime”，LlamaIndex 更像“围绕数据和索引组织的应用框架”。</p><table tabindex="0"><thead><tr><th>层</th><th>核心问题</th></tr></thead><tbody><tr><td>Document / Node</td><td>原始资料如何被切成可检索单元？</td></tr><tr><td>Index</td><td>节点如何进入向量索引、关键词索引或其他存储结构？</td></tr><tr><td>Retriever</td><td>查询如何取回相关节点？</td></tr><tr><td>QueryEngine</td><td>检索结果如何后处理并合成答案？</td></tr><tr><td>Workflow / Agent</td><td>多步检索、工具调用、handoff 如何用事件流组织？</td></tr></tbody></table><p>所以读 LlamaIndex 源码，最好从 RAG query path 开始，而不是直接跳 agent。</p><h2 id="queryengine-主链路" tabindex="-1">QueryEngine 主链路 <a class="header-anchor" href="#queryengine-主链路" aria-label="Permalink to &quot;QueryEngine 主链路&quot;">​</a></h2><p><code>retriever_query_engine.py</code> 可以按这条线读：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>query</span></span>
<span class="line"><span>  -&gt; retriever 取回 nodes</span></span>
<span class="line"><span>  -&gt; node postprocessors 过滤/重排/补 metadata</span></span>
<span class="line"><span>  -&gt; response synthesizer 合成答案</span></span>
<span class="line"><span>  -&gt; callback/event 记录过程</span></span>
<span class="line"><span>  -&gt; response 返回 text + source nodes</span></span></code></pre></div><p>它和第 09 章手写 RAG 的对应关系非常直接：</p><table tabindex="0"><thead><tr><th>第 09 章手写 RAG</th><th>LlamaIndex</th></tr></thead><tbody><tr><td><code>chunk()</code></td><td>Node parsing / ingestion</td></tr><tr><td><code>MemoryVectorStore.search()</code></td><td>Retriever</td></tr><tr><td><code>buildContext()</code></td><td>node postprocessors + synthesizer 输入</td></tr><tr><td><code>generateAnswer()</code></td><td>response synthesizer</td></tr><tr><td>citation/source chunks</td><td>source nodes</td></tr></tbody></table><p>关键判断：LlamaIndex 把“检索后上下文怎么处理”作为 query engine 的核心职责，而不是把它藏在 prompt 里。</p><h2 id="agent-workflow-工具也是数据路径的一部分" tabindex="-1">Agent workflow：工具也是数据路径的一部分 <a class="header-anchor" href="#agent-workflow-工具也是数据路径的一部分" aria-label="Permalink to &quot;Agent workflow：工具也是数据路径的一部分&quot;">​</a></h2><p>读 <code>base_agent.py</code> 时，不要只找“循环”。重点看 agent 如何声明：</p><table tabindex="0"><thead><tr><th>对象</th><th>意义</th></tr></thead><tbody><tr><td>tools</td><td>agent 可以调用的外部能力，也可以是 query engine 封装成的工具。</td></tr><tr><td>events</td><td>workflow 中的状态推进信号。</td></tr><tr><td>handoff</td><td>当前 agent 把控制权交给另一个 agent。</td></tr><tr><td>context</td><td>跨 step 共享状态，而不是散落全局变量。</td></tr></tbody></table><p>这和 LangGraph 的 state channel 很像，但 LlamaIndex 更强调事件和数据工具。</p><h2 id="multiagentworkflow-的读法" tabindex="-1">MultiAgentWorkflow 的读法 <a class="header-anchor" href="#multiagentworkflow-的读法" aria-label="Permalink to &quot;MultiAgentWorkflow 的读法&quot;">​</a></h2><p><code>multi_agent_workflow.py</code> 可以用这条主线：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user input</span></span>
<span class="line"><span>  -&gt; 当前 agent 接收 context</span></span>
<span class="line"><span>  -&gt; agent 选择工具或产出回答</span></span>
<span class="line"><span>  -&gt; 工具结果作为 event 写回</span></span>
<span class="line"><span>  -&gt; 如果需要 handoff，切换 current agent</span></span>
<span class="line"><span>  -&gt; 直到产生 final response</span></span></code></pre></div><p>读它时重点看“current agent”如何被记录和切换。多 agent 的本质不是多个 prompt 放在一起，而是控制权、上下文和事件流要有明确归属。</p><h2 id="workflow-runtime" tabindex="-1">Workflow runtime <a class="header-anchor" href="#workflow-runtime" aria-label="Permalink to &quot;Workflow runtime&quot;">​</a></h2><p><code>workflow.py</code> 是更底层的事件运行时。它回答三个问题：</p><table tabindex="0"><thead><tr><th>问题</th><th>源码里要找</th></tr></thead><tbody><tr><td>step 怎么注册？</td><td>step decorator / step config</td></tr><tr><td>event 怎么推进？</td><td>event dispatch / queue / handler</td></tr><tr><td>context 怎么共享？</td><td>context object / run state</td></tr></tbody></table><p>如果你已经学了 LangGraph，可以把 LlamaIndex Workflow 理解为另一种控制流表达：LangGraph 更像显式图，LlamaIndex Workflow 更像事件驱动 pipeline。</p><h2 id="读源码的顺序" tabindex="-1">读源码的顺序 <a class="header-anchor" href="#读源码的顺序" aria-label="Permalink to &quot;读源码的顺序&quot;">​</a></h2><ol><li>先读 <code>retriever_query_engine.py</code>，把 RAG 查询链路走通。</li><li>找 <code>BaseRetriever</code>、node postprocessor、response synthesizer 的接口边界。</li><li>再读 <code>base_agent.py</code>，看 query engine 如何被包装成 agent 可用工具。</li><li>读 <code>multi_agent_workflow.py</code>，确认 handoff 和 current agent 状态怎么推进。</li><li>最后读 <code>workflow.py</code>，理解通用 event runtime。</li></ol><h2 id="和手写版的对照" tabindex="-1">和手写版的对照 <a class="header-anchor" href="#和手写版的对照" aria-label="Permalink to &quot;和手写版的对照&quot;">​</a></h2><table tabindex="0"><thead><tr><th>本课程手写层</th><th>LlamaIndex 对应层</th></tr></thead><tbody><tr><td>文档切块</td><td>Node parser / ingestion</td></tr><tr><td>向量检索</td><td>retriever</td></tr><tr><td>context builder</td><td>postprocessor + synthesizer 输入</td></tr><tr><td>RAG 回答</td><td>query engine</td></tr><tr><td>tool agent</td><td>agent workflow tools</td></tr><tr><td>多 agent supervisor</td><td>multi agent workflow + handoff</td></tr></tbody></table><h2 id="自检问题" tabindex="-1">自检问题 <a class="header-anchor" href="#自检问题" aria-label="Permalink to &quot;自检问题&quot;">​</a></h2><ul><li>QueryEngine 返回的是纯文本，还是同时携带 source nodes？</li><li>Retriever 和 ResponseSynthesizer 之间为什么需要 node postprocessor？</li><li>一个 query engine 什么时候可以被包装成 tool 给 agent 调用？</li><li>MultiAgentWorkflow 的 handoff 是 prompt 约定，还是 workflow 状态变化？</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("source-analysis/llamaindex.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const llamaindex = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  llamaindex as default
};
