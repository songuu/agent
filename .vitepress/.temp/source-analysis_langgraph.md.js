import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"LangGraph 源码解析","description":"","frontmatter":{},"headers":[],"relativePath":"source-analysis/langgraph.md","filePath":"source-analysis/langgraph.md","lastUpdated":1782716344000}');
const _sfc_main = { name: "source-analysis/langgraph.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="langgraph-源码解析" tabindex="-1">LangGraph 源码解析 <a class="header-anchor" href="#langgraph-源码解析" aria-label="Permalink to &quot;LangGraph 源码解析&quot;">​</a></h1><blockquote><p>一句话：LangGraph 把 agent loop 变成显式状态图。源码主线是 <code>StateGraph</code> 定义图，<code>compile()</code> 生成 Pregel-style runtime，再由 checkpoint、stream、interrupt 把长流程变成可恢复系统。</p></blockquote><p>源码入口按 2026-06-29 官方仓库主干核对：</p><table tabindex="0"><thead><tr><th>入口</th><th>读什么</th></tr></thead><tbody><tr><td><a href="https://github.com/langchain-ai/langgraph/blob/main/libs/langgraph/langgraph/graph/state.py" target="_blank" rel="noreferrer"><code>libs/langgraph/langgraph/graph/state.py</code></a></td><td><code>StateGraph</code>、state schema、channel、reducer、node、edge、compile</td></tr><tr><td><a href="https://github.com/langchain-ai/langgraph/blob/main/libs/langgraph/langgraph/pregel/main.py" target="_blank" rel="noreferrer"><code>libs/langgraph/langgraph/pregel/main.py</code></a></td><td>编译后 runtime 如何按 super-step 执行、stream、checkpoint、resume</td></tr><tr><td><a href="https://github.com/langchain-ai/langgraph/blob/main/libs/prebuilt/langgraph/prebuilt/chat_agent_executor.py" target="_blank" rel="noreferrer"><code>libs/prebuilt/langgraph/prebuilt/chat_agent_executor.py</code></a></td><td><code>create_react_agent</code> 预制 agent 图如何把模型节点和工具节点接成循环</td></tr><tr><td><a href="https://github.com/langchain-ai/langgraph/blob/main/libs/prebuilt/langgraph/prebuilt/tool_node.py" target="_blank" rel="noreferrer"><code>libs/prebuilt/langgraph/prebuilt/tool_node.py</code></a></td><td>tool node 如何读取 tool calls、执行工具、把结果写回 messages</td></tr></tbody></table><h2 id="先抓住三个对象" tabindex="-1">先抓住三个对象 <a class="header-anchor" href="#先抓住三个对象" aria-label="Permalink to &quot;先抓住三个对象&quot;">​</a></h2><table tabindex="0"><thead><tr><th>对象</th><th>心智模型</th></tr></thead><tbody><tr><td><code>StateGraph</code></td><td>图的声明阶段：定义 state、节点、边、条件边。</td></tr><tr><td><code>CompiledStateGraph</code> / Pregel runtime</td><td>图的执行阶段：按边推进节点，合并 partial state，处理 stream/checkpoint。</td></tr><tr><td>prebuilt agent</td><td>框架帮你预接好的图：模型节点 -&gt; 工具节点 -&gt; 模型节点，直到没有 tool call。</td></tr></tbody></table><p>这三层不能混在一起。<code>StateGraph</code> 不是执行器，它只是 DSL；真正跑起来的是 compile 后的 runtime。</p><h2 id="stategraph-的核心-state-reducer" tabindex="-1"><code>StateGraph</code> 的核心：state + reducer <a class="header-anchor" href="#stategraph-的核心-state-reducer" aria-label="Permalink to &quot;\`StateGraph\` 的核心：state + reducer&quot;">​</a></h2><p>手写 agent loop 里，状态通常是几个变量：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">let</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> messages </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [];</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">let</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> steps </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">let</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> finalAnswer </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p>LangGraph 把它提升成 schema 和 channel。每个节点返回 partial update，runtime 决定如何合并：</p><table tabindex="0"><thead><tr><th>channel 行为</th><th>结果</th></tr></thead><tbody><tr><td>默认 replace</td><td>后写覆盖先写，适合 <code>status</code>、<code>route</code>、<code>finalAnswer</code>。</td></tr><tr><td>reducer append</td><td>多个节点写入时累积，适合 <code>messages</code>、<code>events</code>、<code>findings</code>。</td></tr><tr><td>自定义 reducer</td><td>你定义冲突合并规则，适合分数、投票、并行结果归并。</td></tr></tbody></table><p>这就是进阶 LangGraph L1 反复强调的边界：节点不该直接改全局对象，而是返回 partial update。</p><h2 id="compile-后进入-pregel-style-runtime" tabindex="-1">compile 后进入 Pregel-style runtime <a class="header-anchor" href="#compile-后进入-pregel-style-runtime" aria-label="Permalink to &quot;compile 后进入 Pregel-style runtime&quot;">​</a></h2><p>读 <code>pregel/main.py</code> 时，用这条线索：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>input state</span></span>
<span class="line"><span>  -&gt; 找到当前可运行节点</span></span>
<span class="line"><span>  -&gt; 节点读 state，产出 partial update</span></span>
<span class="line"><span>  -&gt; reducer 合并到下一版 state</span></span>
<span class="line"><span>  -&gt; checkpoint/stream 输出本 super-step 结果</span></span>
<span class="line"><span>  -&gt; 根据边和条件边决定下一批节点</span></span>
<span class="line"><span>  -&gt; 直到 END 或达到 recursion limit</span></span></code></pre></div><p>为什么叫 super-step？因为一轮里可能有多个节点并行运行，然后统一合并状态。多 agent 并行 team 的“join 顺序无关”就依赖这个模型。</p><h2 id="prebuilt-create-react-agent-只是预制图" tabindex="-1">prebuilt <code>create_react_agent</code> 只是预制图 <a class="header-anchor" href="#prebuilt-create-react-agent-只是预制图" aria-label="Permalink to &quot;prebuilt \`create_react_agent\` 只是预制图&quot;">​</a></h2><p><code>chat_agent_executor.py</code> 值得读，因为它能打破一个误解：<code>create_react_agent</code> 不是某个神秘 agent，它只是预先搭好的 StateGraph。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>START</span></span>
<span class="line"><span>  -&gt; agent/model node</span></span>
<span class="line"><span>  -&gt; 如果 AIMessage 有 tool_calls: tools node</span></span>
<span class="line"><span>  -&gt; tools node 写回 ToolMessage</span></span>
<span class="line"><span>  -&gt; 回到 agent/model node</span></span>
<span class="line"><span>  -&gt; 如果没有 tool_calls: END</span></span></code></pre></div><p>这和第 04 章手写 ReAct loop 是同一个骨架，只是 LangGraph 把“回到哪一步、状态怎么合并、怎么持久化”变成 runtime 规则。</p><h2 id="tool-node-是工具执行边界" tabindex="-1">tool node 是工具执行边界 <a class="header-anchor" href="#tool-node-是工具执行边界" aria-label="Permalink to &quot;tool node 是工具执行边界&quot;">​</a></h2><p>读 <code>tool_node.py</code> 时重点看四件事：</p><table tabindex="0"><thead><tr><th>步骤</th><th>为什么重要</th></tr></thead><tbody><tr><td>从最后一条 AI message 取 tool calls</td><td>模型只是请求工具，执行权仍在代码。</td></tr><tr><td>解析工具名和参数</td><td>参数仍是不可信输入。</td></tr><tr><td>执行工具并捕获异常</td><td>错误必须变成可回灌状态。</td></tr><tr><td>写回 <code>ToolMessage</code></td><td>下一轮模型节点才能看见 observation。</td></tr></tbody></table><p>这和第 05/06 章完全对应：模型请求调用，本地代码校验和执行，再把 observation 回灌。</p><h2 id="checkpoint-是长流程的分水岭" tabindex="-1">checkpoint 是长流程的分水岭 <a class="header-anchor" href="#checkpoint-是长流程的分水岭" aria-label="Permalink to &quot;checkpoint 是长流程的分水岭&quot;">​</a></h2><p>没有 checkpoint 的图，只是好看的函数调用。加上 checkpoint 后，图才具备生产 runtime 属性：</p><table tabindex="0"><thead><tr><th>能力</th><th>来源</th></tr></thead><tbody><tr><td>同一 <code>thread_id</code> 跨 invoke 续上</td><td>每个 super-step 状态落进 checkpointer。</td></tr><tr><td>Human-in-the-loop 暂停恢复</td><td>interrupt 暂停点必须能恢复。</td></tr><tr><td>time travel / state history</td><td>过去状态可读、可改、可从中间点重放。</td></tr><tr><td>失败后重试</td><td>失败点附近的状态可被定位。</td></tr></tbody></table><p>对照课程：L3 checkpoint、L4 HITL、L5 多 agent 都是建立在同一个 runtime 心智模型上。</p><h2 id="读源码的顺序" tabindex="-1">读源码的顺序 <a class="header-anchor" href="#读源码的顺序" aria-label="Permalink to &quot;读源码的顺序&quot;">​</a></h2><ol><li>从 <code>state.py</code> 读 <code>StateGraph</code> 的 node / edge / conditional edge / compile。</li><li>跳到 <code>pregel/main.py</code>，只追执行循环、stream、checkpoint 相关函数。</li><li>回到 <code>chat_agent_executor.py</code>，看预制 ReAct 图如何使用这些底层能力。</li><li>读 <code>tool_node.py</code>，确认 tool call -&gt; ToolMessage 的闭环。</li><li>对照本仓库 <code>langgraph-advanced/</code> 的 L1-L5，每章只证明一个机制。</li></ol><h2 id="和手写版的对照" tabindex="-1">和手写版的对照 <a class="header-anchor" href="#和手写版的对照" aria-label="Permalink to &quot;和手写版的对照&quot;">​</a></h2><table tabindex="0"><thead><tr><th>本课程手写层</th><th>LangGraph 对应层</th></tr></thead><tbody><tr><td><code>while (steps &lt; maxSteps)</code></td><td>条件边 + recursionLimit</td></tr><tr><td><code>messages.push(...)</code></td><td>messages channel + reducer</td></tr><tr><td><code>toolRegistry.run(...)</code></td><td><code>ToolNode</code></td></tr><tr><td><code>onStep</code> 输出</td><td>stream modes / event stream</td></tr><tr><td>手动保存上下文</td><td>checkpointer + thread_id</td></tr><tr><td>人工确认 if/else</td><td>interrupt + Command(resume)</td></tr></tbody></table><h2 id="自检问题" tabindex="-1">自检问题 <a class="header-anchor" href="#自检问题" aria-label="Permalink to &quot;自检问题&quot;">​</a></h2><ul><li>一个节点返回 partial update 时，哪些字段会 replace，哪些字段会 append？</li><li><code>create_react_agent</code> 里的循环边在哪里闭合？</li><li>checkpoint 是保存模型回答，还是保存每个 super-step 的状态？</li><li>并行节点同时写同一个 channel 时，reducer 如何保证结果可预测？</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("source-analysis/langgraph.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const langgraph = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  langgraph as default
};
