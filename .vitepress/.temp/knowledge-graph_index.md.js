import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"知识图谱系统","description":"","frontmatter":{},"headers":[],"relativePath":"knowledge-graph/index.md","filePath":"knowledge-graph/README.md","lastUpdated":1781146684000}');
const _sfc_main = { name: "knowledge-graph/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="知识图谱系统" tabindex="-1">知识图谱系统 <a class="header-anchor" href="#知识图谱系统" aria-label="Permalink to &quot;知识图谱系统&quot;">​</a></h1><p>一套<strong>数据驱动、可持续扩展</strong>的知识图谱：一张全局概念图谱、每章各自的图谱、一个交互式「动图」，以及随时可增补的关联文章。</p><h2 id="一条命令生成全部" tabindex="-1">一条命令生成全部 <a class="header-anchor" href="#一条命令生成全部" aria-label="Permalink to &quot;一条命令生成全部&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> kg</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # = npx tsx knowledge-graph/generate.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> kg</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --no-inject</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 只生成全局 md + 交互 HTML，不改任何章节 README</span></span></code></pre></div><p>会产出 / 更新：</p><table tabindex="0"><thead><tr><th>产物</th><th>路径</th><th>说明</th></tr></thead><tbody><tr><td>全局图谱</td><td><a href="./../docs/knowledge-graph"><code>docs/knowledge-graph.md</code></a></td><td>Mermaid 章节地图 + 概念图 + 概念索引 + 文章索引（GitHub 直接渲染）</td></tr><tr><td>交互图谱（动图）</td><td><code>knowledge-graph/output/index.html</code></td><td>Cytoscape.js，缩放/拖拽/按部分筛选/点节点看关联文章。下载后用浏览器打开</td></tr><tr><td>每章图谱</td><td>各 <code>lessons/NN-*/README.md</code> 的「知识图谱与延伸阅读」节</td><td>本章概念子图 + 跨章关系 + 延伸阅读，写在 <code>&lt;!-- KG:START/END --&gt;</code> 标记区</td></tr></tbody></table><h2 id="唯一事实来源" tabindex="-1">唯一事实来源 <a class="header-anchor" href="#唯一事实来源" aria-label="Permalink to &quot;唯一事实来源&quot;">​</a></h2><p>知识图谱内容来自 <a href="https://github.com/songuu/agent/blob/master/knowledge-graph/data/graph.ts" target="_blank" rel="noreferrer"><code>knowledge-graph/data/graph.ts</code></a>。</p><ul><li><code>CHAPTERS</code> — 章节清单（一般不动）。</li><li><code>CONCEPTS</code> — 概念节点：<code>{ id, label, chapter, summary? }</code>。</li><li><code>RELATIONS</code> — 概念关系：<code>{ from, to, type, note? }</code>，<code>type ∈ 前置|深化|对比|应用|组成</code>。</li><li><code>ARTICLES</code> — 关联文章：<code>{ title, url, kind, chapters[], note? }</code>，<code>kind ∈ paper|doc|blog|video|internal</code>。</li></ul><p>网页里的「抽象概念可视化」模块来自 <a href="https://github.com/songuu/agent/blob/master/knowledge-graph/data/visuals.ts" target="_blank" rel="noreferrer"><code>knowledge-graph/data/visuals.ts</code></a>。它按 <code>chapter</code> 绑定到 <code>CHAPTERS.id</code>，由 VitePress 构建期自动注入到章节页第一张学习地图后；课程 README 正文不需要手动插 HTML。模块同时渲染代码内联绘制的 SVG 图片、「核心判断 / 易错边界」彩色加粗重点，以及来自 <code>ARTICLES</code> 的外部理解链接。<code>visuals.test.mts</code> 会强制检查每个课程都有 Mermaid 流程图或思维导图、Codex 绘制的 visual 模块、2 条重点标注和至少 1 条外部引用。</p><p>所有网页 Mermaid 图（流程图 / 思维导图）由 <code>.vitepress/theme/diagram-zoom.ts</code> 在客户端增强：支持按钮缩放、<code>Ctrl/⌘ + wheel</code> 缩放、拖拽平移和一键重置。Markdown 中仍保留标准 Mermaid 代码块，便于 GitHub/Obsidian 阅读。</p><h2 id="怎么扩展" tabindex="-1">怎么扩展 <a class="header-anchor" href="#怎么扩展" aria-label="Permalink to &quot;怎么扩展&quot;">​</a></h2><p><strong>新增一篇延伸文章</strong>：在 <code>ARTICLES</code> 里加一条，写明它关联哪些章节，跑 <code>npm run kg</code>。该文章会自动出现在全局文章索引、相关章节的「延伸阅读」、以及交互图谱里这些章节概念的详情面板中。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  title</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Lost in the Middle: How Language Models Use Long Contexts&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  url</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://arxiv.org/abs/2307.03172&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;paper&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  chapters</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;07&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;09&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 关联到「短期记忆」「RAG」两章</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  note</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;长上下文中信息位置如何影响利用率——记忆/检索章的重要背景。&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">},</span></span></code></pre></div><p><strong>新增一个概念 / 一条关系</strong>：在 <code>CONCEPTS</code> / <code>RELATIONS</code> 加条目（概念 id 全局唯一、kebab-case），跑 <code>npm run kg</code>，全局图、本章图、交互图同步更新。本章概念图谱会自动：<strong>橙框</strong>高亮本章概念、蓝框弱化关联的他章概念、连线按关系类型配色（前置=蓝 / 深化=紫 / 对比=玫红 / 应用=绿 / 组成=橙）；节点数超过 8 时从 <code>LR</code> 切到 <code>TB</code>（思维导图式自上而下），降低密集章节的横向拥挤。这些规则在 <code>buildChapterConceptGraph</code>（纯函数，可单测）里，改动后跑 <code>npx tsx knowledge-graph/generate.test.mts</code>。</p><p><strong>新增一个抽象概念可视化模块</strong>：在 <code>CONCEPT_VISUALS</code> 中加一条，选择 <code>kind</code>（<code>loop</code> / <code>pipeline</code> / <code>fusion</code> / <code>space</code> / <code>compare</code> / <code>layers</code> / <code>stream</code> / <code>shield</code>），写明 <code>chapter</code>、<code>title</code>、<code>summary</code>、<code>steps</code>、<code>takeaway</code>；在 <code>CONCEPT_HIGHLIGHTS</code> 中补「核心判断 / 易错边界」；在 <code>ARTICLES</code> 中保证该章至少有一条外部理解链接。每个 <code>kind</code> 都会渲染一套专属的内联 SVG 动画场景（如 <code>loop</code>=椭圆循环环、<code>pipeline</code>=传送带、<code>fusion</code>=双路汇流、<code>space</code>=语义空间、<code>compare</code>=对比卡片、<code>layers</code>=分层、<code>stream</code>=流式 token），动画纯 CSS 且在 <code>prefers-reduced-motion</code> 下静止。visual 图片必须继续由 <code>visuals.ts</code> 生成内联 SVG，不新增外部图片资源。再跑 <code>npx tsx knowledge-graph/data/visuals.test.mts</code>、<code>npx tsx knowledge-graph/generate.test.mts</code>、<code>npx tsx .vitepress/theme/diagram-zoom.test.mts</code> 和 <code>pnpm site:build</code>。新增课程时必须同步补 README 的 Mermaid 流程图或思维导图和这里的 visual 数据。</p><h2 id="设计要点" tabindex="-1">设计要点 <a class="header-anchor" href="#设计要点" aria-label="Permalink to &quot;设计要点&quot;">​</a></h2><ul><li><strong>幂等</strong>：<code>generate.ts</code> 可反复运行；README 只替换 <code>&lt;!-- KG:START/END --&gt;</code> 标记之间的内容，不动你的正文。</li><li><strong>零新增依赖</strong>：交互图用 Cytoscape.js 走 CDN，不进 <code>package.json</code>。</li><li><strong>数据非法即报错</strong>：概念引用了不存在的章节、关系引用了不存在的概念、文章关联了不存在的章节，生成器会 fail-fast 并指出哪一条。</li><li><strong>与「图解学习地图」互补</strong>：每章原有的 <code>图解学习地图</code> 讲的是本章流程；知识图谱讲的是概念之间的关系与跨章脉络。</li><li><strong>视觉解释不改正文</strong>：「抽象概念可视化」由 <code>visuals.ts</code> 数据驱动，样式集中在 <code>.vitepress/theme/custom.css</code>；Mermaid 缩放增强集中在 <code>.vitepress/theme/diagram-zoom.ts</code>，适合反复补图、流程、动画、重点标注和外部阅读入口而不污染课程 Markdown。</li><li><strong>清晰度优先</strong>：Mermaid 字号/字体/间距在 <code>.vitepress/config.mts</code> 的 <code>mermaid</code> 配置里统一调（<code>useMaxWidth:false</code> 让图按本征尺寸渲染，交给缩放层适配，文字不被压小）；<code>diagram-zoom.ts</code> 控制首屏可读尺度（小图放大、宽图至少 100% 开屏）与画布高度。改这些常量须同步更新 <code>diagram-zoom.test.mts</code>。不写死 Mermaid <code>theme</code>，保留明暗模式自适应。</li></ul><blockquote><p>⚠️ 外部文章链接请自行核实有效性——本系统只负责把它们组织好，不保证 URL 长期可用。</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("knowledge-graph/README.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const README = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  README as default
};
