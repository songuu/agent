# vitepress-plugin-mermaid 在 dev 下把 mermaid 的依赖（cytoscape / dayjs / d3 / debug /
# @braintree/sanitize-url 等）写进 Vite 的 optimizeDeps.include，要求它们在【顶层】
# node_modules 可解析。pnpm 默认严格、不提升传递依赖，导致 dev 报
# "Failed to resolve dependency" → mermaid 加载失败 → 客户端整页白屏
# （build / preview 不受影响，因 rollup 走真实依赖树）。
# shamefully-hoist 把传递依赖铺平到顶层（与 npm 行为一致），一次性修掉这类解析失败，
# 避免逐个 public-hoist-pattern 的 whack-a-mole。课程本身用 tsx 直跑，铺平不影响其解析。
shamefully-hoist=true
